name: Veille pickup sans LLM

on:
  schedule:
    - cron: "17 */6 * * *"
  workflow_dispatch:
    inputs:
      deploy_validated_snapshot:
        description: "Publier l'instantané validé sans relancer le scan"
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pickup-pages
  cancel-in-progress: false

jobs:
  scan-and-publish:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Restaurer l'historique du tableau
        if: ${{ github.event_name == 'schedule' || inputs.deploy_validated_snapshot != true }}
        uses: actions/cache/restore@v4
        with:
          path: docs/data
          key: pickup-data-${{ github.run_id }}
          restore-keys: pickup-data-

      - name: Scanner les annonces sans LLM
        if: ${{ github.event_name == 'schedule' || inputs.deploy_validated_snapshot != true }}
        run: python3 scan_deals.py --timeout 20

      - name: Conserver l'historique du prochain scan
        if: ${{ github.event_name == 'schedule' || inputs.deploy_validated_snapshot != true }}
        uses: actions/cache/save@v4
        with:
          path: docs/data
          key: pickup-data-${{ github.run_id }}

      - name: Préparer GitHub Pages
        uses: actions/configure-pages@v5

      - name: Charger l'artefact statique
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

      - name: Publier GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
